{
  "name": "eidolon-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "uvicorn app:app --reload",
    "lint": "ruff check && ruff format",
    "typecheck": "mypy .",
    "test": "pytest"
  },
  "dependencies": {
    "fastapi": "^0.110.0",
    "uvicorn": "^0.27.0",
    "pg": "^3.9.9",
    "redis": "^5.0.5",
    "pydantic": "^2.7.0",
    "pydantic-settings": "^2.2.0"
  },
  "devDependencies": {
    "pytest": "^8.0.0",
    "ruff": "^0.1.0",
    "mypy": "^1.7.0"
  }
}